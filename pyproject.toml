[build-system]
requires=["hatchling>=1.26"]
build-backend="hatchling.build"

[project]
name="dcc-mcp-sketchup"
version="0.1.0"
description="SketchUp adapter foundation for DCC-MCP"
readme="README.md"
requires-python=">=3.9"
license={text="MIT"}
authors=[{name="loonghao",email="hal.long@outlook.com"}]
dependencies=["dcc-mcp-core>=0.19.45,<1.0.0"]

[project.optional-dependencies]
test=["pytest>=8","ruff>=0.8"]

[project.scripts]
dcc-mcp-sketchup="dcc_mcp_sketchup.server:main"

[project.entry-points."dcc_mcp.adapters"]
sketchup="dcc_mcp_sketchup:SketchupMcpServer"

[tool.hatch.build.targets.wheel]
packages=["src/dcc_mcp_sketchup"]
artifacts=["src/dcc_mcp_sketchup/skills/**"]

[tool.pytest.ini_options]
testpaths=["tests"]
pythonpath=["src","."]

[tool.ruff.lint]
select=["E","F","I","B"]
